<!DOCTYPE html>
<html>
<head>
    
</head>
<body>
    <div style="width: 300px; margin: 0 auto;"> <!-- Set inline styles for form container -->
        {{ form_start(form , {attr: {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
</body>
</html>

